export default function CountryLayout({ children }) {
    return (
    <html lang="en" className="scroll-smooth">
        <body className="layout__select__country">
          {children}
        </body>
  </html>
  )
}